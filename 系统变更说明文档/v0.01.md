# v0.01 版本变更说明

> 发布日期：2026-06-04　|　对应分支/标签：main　|　负责人：henry + AI（技术总监）
> 主题：**项目基线 —— 技术方案、开发规范与文档体系**

## 一、本次变更

### 新增（feat / docs）
- 确定项目定位：轻量级数据共享平台，核心"把 SQL 跑成 Excel 提供下载"。
- 确定技术栈：Django + DRF / Vue 3 + Element Plus / APScheduler / SQLite / Oracle（python-oracledb thin）。
- 新增 `docs/技术方案.md`（v0.3）：架构、数据模型、模块划分、安全方案、复杂度地图、版本边界。
- 新增 `docs/开发规范.md`（v1.1）：协作约定、需求/变更/删除红线、**三维度审核**、编码规范（按官方文档）、安全红线、Git 与版本规范、文档维护防腐。
- 新增 `README.md`：项目总入口与各文件作用索引。
- 新增 `系统变更说明文档/`（README + 模板 + 本文件）。
- 新增 `当前系统文档/系统架构.md`：当前架构快照。
- 新增 `.gitignore`、`.gitattributes`，初始化 Git 仓库并接入 GitHub 远程。

### 修改 / 优化
- 无（首个版本）。

### 修复
- 无。

### 删除
- 无。

## 二、下一版本计划（v0.02）

- 目标：**搭建后端项目骨架 + 固化安全核心**。
- 待办：
  - [ ] 初始化 Django 项目（`backend/`）、`requirements.txt`（锁版本）、`.env.example`。
  - [ ] `core/oracle_client.py`：只读校验 + 绑定变量 + `fetchmany` 流式（含单元测试）。
  - [ ] `core/excel.py`：openpyxl 导出。
  - [ ] 配置 `ruff` + `black`。
  - [ ] 完成后过三维度审核，并更新本目录与当前系统文档。

## 三、风险与注意事项

- 代码尚未开始，架构以技术方案 v0.3 为准（主体稳定、可微调）。
- 安全核心（只读/绑定变量/行数上限）须在 v0.02 即落地，不后置。
